<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= user_signed_in? ? feed_posts_path : root_path %>">Notifications</a>
    </div>
    
    <div id="navbar" class="navbar-collapse collapse">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li class="<%= "active" if controller?("users") and action?("index") %>"><a href="<%= users_path %>">Users</a></li>
          <li class= "<%= "active" if controller?("users") and action?("show") %>"><a href="<%= user_path(current_user) %>">Contact</a></li>
          <li class= "<%= "active" if controller?("posts") %>" ><a href="<%= new_post_path %>">Post</a></li>
        </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown open-messages">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true">
              <span class="fa fa-paper-plane" aria-hidden="true"></span>
              <span class="badge badge-success" id="msg-notification-count" aria-hidden="true"><%= current_user.message_notifications.where(seen: false).count %></span>
            </a>
            <ul class="dropdown-menu w300" id="message-table" role="menu">
              <div>
                <table id = "message-table">
                  <tbody id = "user-message-list">
                    
                  </tbody>
                </table>
              </div>
              <div class="hidden" id = "msg-notification-pagination">
                
              </div>
            </ul>
          </li>
          
          <li class="dropdown open-notification">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true">
              <span class="fa fa-bell-o fs14 va-m" aria-hidden="true"></span>
              <span class="badge badge-danger" id="notification-count" aria-hidden="true"><%= current_user.notifications.where(seen: false).count %></span>
            </a>
            <ul class="dropdown-menu w300" id="notification-table" role="menu">
              <div>
                <table id = "notification-table">
                  <tbody id = "user-notification-list">
                    
                  </tbody>
                </table>
              </div>
              <div class="hidden" id = "notification-pagination">
                
              </div>
            </ul>
          </li>
          

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="<%= profile_user_path(current_user) %>" > Profile</a></li>
              <li><a href="<%= edit_user_registration_path %>">Account</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="<%= destroy_user_session_path %>" data-method="delete">Logout</a></li>
            </ul>
          </li>
        <% else %>
          <li><a href="<%= new_user_session_path %>" > Sign in </a></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
    

  </div>
</nav>