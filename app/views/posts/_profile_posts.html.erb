<% @posts.each do |post| %>

	<div class="post-content"> 
		<%= link_to (raw("<i class='fa fa-pencil-square-o'></i>")),edit_post_path(post),class: 'button post-edit-button' %>
		<%= post.content.html_safe %>
	</div>

<% end %>